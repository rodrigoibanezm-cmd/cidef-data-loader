name: Patch competitive trajectory ROMs

on:
  push:
    paths:
      - '.github/workflows/patch-competitive-trajectory-roms.yml'

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Test trajectory helpers
        run: node --test test/competitive-share-trajectory.test.js
      - name: Patch ROM files
        run: node scripts/patch-competitive-trajectory-roms.mjs
      - name: Commit ROM changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rom/schema.json rom/motors.md
          if ! git diff --cached --quiet; then
            git commit -m "Document competitive share trajectory"
            git push
          fi
