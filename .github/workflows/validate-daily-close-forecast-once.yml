name: One-time daily close forecast validation

on:
  push:
    paths:
      - '.github/workflows/validate-daily-close-forecast-once.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts --no-audit --no-fund
      - name: Run targeted tests
        run: node --test test/daily-close-forecast-backtest.test.js test/daily-close-backtest-context.test.js
